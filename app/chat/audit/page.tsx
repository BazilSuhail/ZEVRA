"use client";

import { useState } from "react";
import { FiCheckCircle, FiClock, FiLock, FiShield } from "react-icons/fi";

const events = [["Session unlocked", "Today, 09:42", "Your local demo session is active."], ["Message decrypted", "Today, 09:40", "A secure message was opened locally."], ["New device previewed", "Yesterday", "No network request was made."]];

export default function AuditPage() { const [tab, setTab] = useState<"activity" | "security">("activity"); return <div className="flex flex-1 flex-col overflow-y-auto bg-[#fbfcfd] dark:bg-zinc-950"><header className="border-b border-zinc-200 bg-white px-6 py-6 dark:border-zinc-800 dark:bg-zinc-900"><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-500">Local demo</p><h1 className="mt-1 text-2xl font-bold">Security center</h1><p className="mt-1 text-sm text-zinc-500">A visual preview of local security events.</p></header><div className="border-b border-zinc-200 bg-white px-6 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex gap-6">{(["activity", "security"] as const).map((key) => <button key={key} onClick={() => setTab(key)} className={`border-b-2 py-3 text-sm font-semibold ${tab === key ? "border-indigo-600 text-indigo-600" : "border-transparent text-zinc-400"}`}>{key === "activity" ? "Activity log" : "Security events"}</button>)}</div></div><div className="mx-auto w-full max-w-2xl space-y-3 p-6">{events.map(([title, time, detail]) => <div key={title} className="flex gap-4 rounded-xl border border-zinc-200 bg-white p-4 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-emerald-50 text-emerald-600 dark:bg-emerald-950/40"><FiCheckCircle /></div><div><p className="font-semibold">{tab === "security" ? title.replace("Session", "Device") : title}</p><p className="mt-1 text-xs text-zinc-500">{detail}</p><p className="mt-2 flex items-center gap-1 text-[11px] text-zinc-400"><FiClock /> {time}</p></div></div>)}<div className="flex items-center gap-2 rounded-xl bg-indigo-50 p-4 text-xs text-indigo-700 dark:bg-indigo-950/30 dark:text-indigo-300"><FiLock /> All events shown here are mock data for the UI preview.</div></div></div>; }
