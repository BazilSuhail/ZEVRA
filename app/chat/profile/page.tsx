"use client";

import { useState } from "react";
import { FiCheck, FiEdit2, FiMail, FiUser } from "react-icons/fi";

export default function ProfilePage() { const [name, setName] = useState("Mina Chen"); const [editing, setEditing] = useState(false); const [saved, setSaved] = useState(false); const save = () => { setEditing(false); setSaved(true); setTimeout(() => setSaved(false), 1600); }; return <div className="flex flex-1 flex-col overflow-y-auto bg-[#fbfcfd] dark:bg-zinc-950"><header className="border-b border-zinc-200 bg-white px-6 py-6 dark:border-zinc-800 dark:bg-zinc-900"><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-500">Local demo</p><h1 className="mt-1 text-2xl font-bold">Profile</h1></header><div className="mx-auto w-full max-w-2xl p-6"><div className="rounded-xl border border-zinc-200 bg-white p-6 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-amber-100 text-xl font-bold text-amber-700">MC</div><div className="flex-1">{editing ? <input autoFocus value={name} onChange={(event) => setName(event.target.value)} className="rounded-lg border border-indigo-300 px-3 py-2 text-lg font-semibold outline-none" /> : <h2 className="text-xl font-bold">{name}</h2>}<p className="mt-1 flex items-center gap-1 text-sm text-zinc-500"><FiMail /> mina@example.test</p></div><button onClick={() => editing ? save() : setEditing(true)} className="rounded-lg bg-indigo-50 p-2 text-indigo-600 dark:bg-indigo-950/40">{editing ? <FiCheck /> : <FiEdit2 />}</button></div>{saved && <p className="mt-5 flex items-center gap-2 text-xs font-semibold text-emerald-600"><FiCheck /> Profile preview updated</p>}<div className="mt-7 grid gap-3 sm:grid-cols-2"><div className="rounded-lg bg-zinc-50 p-4 dark:bg-zinc-800"><FiUser className="mb-3 text-zinc-400" /><p className="text-xs text-zinc-500">Account type</p><p className="mt-1 text-sm font-semibold">Local prototype</p></div><div className="rounded-lg bg-zinc-50 p-4 dark:bg-zinc-800"><FiCheck className="mb-3 text-emerald-500" /><p className="text-xs text-zinc-500">Connection</p><p className="mt-1 text-sm font-semibold">Offline by design</p></div></div></div></div></div>; }
