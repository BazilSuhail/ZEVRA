"use client";

import { useState } from "react";
import { FiBell, FiMoon, FiSun } from "react-icons/fi";

export default function SettingsPage() { const [dark, setDark] = useState(true); const [notifications, setNotifications] = useState(true); return <div className="flex flex-1 flex-col overflow-y-auto bg-[#fbfcfd] dark:bg-zinc-950"><header className="border-b border-zinc-200 bg-white px-6 py-6 dark:border-zinc-800 dark:bg-zinc-900"><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-500">Local demo</p><h1 className="mt-1 text-2xl font-bold">Settings</h1><p className="mt-1 text-sm text-zinc-500">These controls affect the preview only.</p></header><div className="mx-auto w-full max-w-2xl space-y-4 p-6"><div className="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex items-center justify-between"><div className="flex items-center gap-3">{dark ? <FiMoon className="text-indigo-600" /> : <FiSun className="text-amber-500" />}<div><p className="font-semibold">Appearance</p><p className="text-xs text-zinc-500">{dark ? "Dark mode" : "Light mode"}</p></div></div><button onClick={() => setDark(!dark)} className="rounded-lg bg-zinc-100 px-3 py-2 text-xs font-semibold dark:bg-zinc-800">Switch</button></div></div><div className="rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><FiBell className="text-indigo-600" /><div><p className="font-semibold">Notifications</p><p className="text-xs text-zinc-500">Message alerts in this preview</p></div></div><button onClick={() => setNotifications(!notifications)} className={`h-6 w-11 rounded-full p-1 ${notifications ? "bg-indigo-600" : "bg-zinc-300"}`}><span className={`block h-4 w-4 rounded-full bg-white transition-transform ${notifications ? "translate-x-5" : ""}`} /></button></div></div></div></div>; }
